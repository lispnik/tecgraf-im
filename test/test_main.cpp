/* doctest entry point. Kept in its own translation unit so the header's
 * implementation is compiled once and the actual test files stay cheap to
 * rebuild. */

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest/doctest.h"
